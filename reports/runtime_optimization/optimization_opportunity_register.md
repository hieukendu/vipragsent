# Optimization opportunity register

Candidates only; none are approved or measured. All require identity reconciliation and correctness evidence first. Saved-time ranges are intentionally `NOT_ESTIMABLE` until authorized fixture/profile evidence exists; qualitative critical-path scenarios are not measured claims.

- `O-P0-01` — P0, checkpoint/resume: atomic checkpoint/resume contract and interruption recovery. Evidence: clean-base `training/checkpoints.py` is canonical, generation executor has a separate direct writer/loader and late epoch checkpoint. Baseline role: protects training critical path and prevents replay. Saved time: NOT_ESTIMABLE; scenario is zero duplicated work after a safe boundary. Risk: high state/provenance risk; owner `LUNA_COT_RESUME`/`LUNA_CHECKPOINT_RECOVERY`.
- `O-P0-02` — P0, generation: batched Vistral inference with variable-length padding, per-sample stopping, reversible context and a fixture output-equivalence harness. Evidence: clean-base generation loops per record. Baseline role: GPU generation critical path. Saved time: NOT_ESTIMABLE; scenario is reduced launch/padding overhead only if equivalence passes. Risk: high scientific/output risk; owner `LUNA_GENERATION_ENGINE`.
- `O-P0-03` — P0, persistence: incremental, idempotent chunk commits before judging and crash recovery. Evidence: clean-base split artifacts are written after the complete split. Baseline role: failure/replay/storage boundary. Saved time: NOT_ESTIMABLE; scenario is bounded replay after interruption. Risk: high ordering/idempotency risk; owner `LUNA_GENERATION_ENGINE`.
- `O-P1-01` — P1, reuse: hash/provenance-bound reuse of existing DEV/checkpoint artifacts, with best-epoch DEV artifact reuse only after selection. Evidence: epoch-2 state/checkpoint exists, but run-to-source binding conflicts. Saved time: NOT_ESTIMABLE; scenario is avoided duplicate DEV work after exact hash match. Risk: high scientific identity risk; owner `LUNA_HF_REUSE`/`LUNA_COT_RESUME`; decision `BLOCKED_PENDING_RECONCILIATION`.
- `O-P1-02` — P1, Azure/judge: bounded async mock-only queue/backpressure/cache identity and explanation-only continuation. Evidence: clean-base judge uses per-row calls and cache; no real Azure is allowed. Saved time: NOT_ESTIMABLE; scenario is bounded concurrency after mock contract tests. Risk: high spend/order risk; owner `LUNA_AZURE_PIPELINE`.
- `O-P1-03` — P1, scheduling: resource-aware DAG for one 7B job, encoder overlap only where ownership and DEV dependencies allow, with partial telemetry treated conservatively. Evidence: current one-GPU policy and H100 MIG telemetry. Saved time: NOT_ESTIMABLE; scenario is overlap of independent permitted paths only. Risk: medium resource/scientific dependency risk; owner `LUNA_SCHEDULER`.
- `O-P2-01` — P2, estimation/profile: runtime estimator and authorized microbenchmark plan with 1/1.5/2/2.5/3/4 sensitivity. Evidence: no measured Wave-0 baseline. Saved time: NOT_ESTIMABLE; decision `AUDIT_ONLY` until authorization. Risk: low code risk but high false-precision risk; owner `LUNA_RUNTIME_ESTIMATOR`.

No model/data benchmark, production execution, Azure call, HF write, or source edit is part of this register’s Wave-0 scope.

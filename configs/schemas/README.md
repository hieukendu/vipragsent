# Frozen schemas

The JSON schemas in this directory are the machine-readable contract for persisted predictions, run manifests,
Azure labels, rationales, and final results. They use only the canonical label keys from `configs/labels.json`.
